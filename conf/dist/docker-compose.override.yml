version: '3.7'
# services:
#   boxed-out-boilerplate:
#     command: /bin/sh -c "while sleep 1000; do :; done"
#     build:
#      context: .
#      target: dev
#      args:
#        APP_ID: manage-panel
#        GITHUB_TOKEN: ${GITHUB_TOKEN}
#        DOCKER_ENV: 'development'
#        STAGE: 'dev',
#        USER_ID: 1000
#        GROUP_ID: 1000
#     image: 'local/boxedout-core-api' # disable the image from the parent yml
#     # full volumes to improve performance
#     volumes:
#       # folders of the parent directories must be available for the
#       # creation of symlinks
#       - ../:/opt/deps:delegated
#       # performance fix on non-linux OS
#       - server-src:/opt/application
#       - server-tmp:/tmp
#       - server-home:/home/node
