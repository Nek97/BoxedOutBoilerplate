import { BoxedOutStringFormatEnum } from '../app-helpers/boxedout-string-format.enum';

describe('BoxedOut String Format enum test', () => {
  it('Check definition', async () => {
    expect(BoxedOutStringFormatEnum).toBeDefined();
  });
});
